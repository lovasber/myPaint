export enum actionName {
    DRAWING = 'drawing',
    SELECTING = 'selecting',
    MOVING = 'moving',
    TODELETE = 'todelete',
}

export enum shape {
    RECTANGLE = 'rectangle',
    LINE = 'line',
}